# --- Day 1: ingestion slice ---
yt-dlp>=2026.8.19
opencv-python>=4.10
python-dotenv>=1.0

# --- Day 2: STT + VLM captioning ---
faster-whisper>=1.0
requests>=2.31        # talks to local Ollama HTTP API

# --- Day 3+: note gen / index / UI ---
streamlit>=1.38
# sqlite3 is Python stdlib — no install needed
